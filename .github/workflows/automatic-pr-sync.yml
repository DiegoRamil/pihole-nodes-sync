name: "Automatic PR Sync"
on:
  push:
    branches:
      - master
      - develop

permissions:
  contents: write
  pull-requests: write

jobs:
  pr_sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@ff7abcd0c3c05ccf6adc123a8cd1fd4fb30fb493

      - name: Sync all PRs
        uses: DiegoRamil/actions-pr-auto-update@34a3d0c3c5551fde0491c4598c5f486d73ef8bc1
        with:
          token: ${{ secrets.GIT_TOKEN }}
          include_drafts: true
          exclude_labels: "skip auto update, still in progress"
